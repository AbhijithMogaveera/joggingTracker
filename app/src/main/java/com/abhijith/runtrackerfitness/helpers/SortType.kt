package com.abhijith.runtrackerfitness.helpers

/**
 * Describes the sort type of the run list
 */
enum class SortType {
    DATE,
    RUNNING_TIME,
    DISTANCE,
    AVG_SPEED,
    CALORIES_BURNED

}